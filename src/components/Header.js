import React from 'react';
import MainNav from './MainNav';

const Header = () => {
    return (
        <header>
            <strong>My Header</strong>
            <MainNav/>
        </header>
    );
}

export default Header;